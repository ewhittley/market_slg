<%= form_for(@basket, :html => {class: "form-horizontal", role: "form"}) do |f| %>
  <div>
    <% if user_signed_in? %>
      <%= f.submit %>
    <% else %>
      You must Sign up or Sign in to create a basket
    <% end %>
  </div>
<% end %>
